[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dynamic-parking"
version = "0.1.0"
description = "동적 그래프 기반 실시간 주차 라우팅 알고리즘 라이브러리"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]